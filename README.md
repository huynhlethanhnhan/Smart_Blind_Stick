# 1. Thêm file vừa tạo vào danh sách chờ
git add README.md

# 2. Lưu thay đổi với lời nhắn
git commit -m "Add README with instructions"

# 3. Đẩy lên GitHub (dùng --force nếu bạn muốn ghi đè hoàn toàn để tránh lỗi conflict cũ)
git push origin main
